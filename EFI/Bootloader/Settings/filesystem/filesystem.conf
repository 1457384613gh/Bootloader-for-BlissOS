#######################################################
#	    [filesystem.config] of [Bootloader for BlissOS]
#	Configuration file for loading EFI file system drivers
#######################################################

scan_driver_dirs EFI/Bootloader/Settings/filesystem

#######################################################
##############    {README written by Chauncy}    ###############
#######################################################
#
#[f2fs_x64.efi] is experimental. 
#If f2fs partition is NOT found, 
#	- mount it and then unmount it by using linux ditro.
#
#######################################################
###########    README written by Roderick W. Smith    ############
#######################################################

##########    {set to scan where EFI drivers are}    ###############
#######################################################
# Directories in which to search for EFI drivers. These drivers can
# provide filesystem support, give access to hard disks on plug-in
# controllers, etc. In most cases none are needed, but if you add
# EFI drivers and you want rEFInd to automatically load them, you
# should specify one or more paths here. rEFInd always scans the
# "drivers" and "drivers_{arch}" subdirectories of its own installation
# directory (where "{arch}" is your architecture code); this option
# specifies ADDITIONAL directories to scan.
# Default is to scan no additional directories for EFI drivers
#######################################################